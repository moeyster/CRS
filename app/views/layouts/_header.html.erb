<div class="col-sm-12">
		<% if agent_signed_in? %>
		    <h3 class="text-center"><i class="glyphicon glyphicon-dashboard"></i> CRS<small>v1.0</small> | <small>Welcome <strong><%= current_agent.name %></strong></small></h3>
		    <hr>
		<% else %>
			<h3 class="text-center"><i class="glyphicon glyphicon-dashboard"></i> CRS<small>v1.0</small></h3>
		    <hr>
		<% end %>
    <%= render 'layouts/message' %>
</div>
